{% extends 'layouts.admin.html.twig' %}

{% block content %}
    <!-- Main Content -->
    <div class="container">
        <div class="row">
            <a class="btn btn-primary" href="/addUser">Ajouter</a>
            <table class="table tablesorter tablesorter-default" role="grid">
                <thead>
                <tr>
                    <th>Identifiant</th>
                    <th>Nom</th>
                    <th>Adresse de messagerie</th>
                    <th>Rôle</th>
                    <th>action</th>
                </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>{{ user.firstname }} {{ user.lastname }}</td>
                        <td>{{ user.email }}</td>
                        <td>Mon role</td>
                        <td>
                            <a href="/changeUser?id={{ user.userID.value }}" class="btn btn-primary">modifier</a>
                            <a href="#" class="btn btn-danger">Verrouiller</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock content %}


{% block actionTitle %}Utilisateurs{% endblock actionTitle %}
