{% extends 'layouts.admin.html.twig' %}

{% block content %}
<!-- Main Content -->
<div class="container">
    <div class="row">
        <table class="table tablesorter tablesorter-default" role="grid">
            <thead>
                <tr>
                    <th>Author</th>
                    <th>Commentary</th>
                    <th>Post</th>
                    <th>Date</th>
                </tr>
            </thead>
            <tbody>
                {% for commentAggregate in comments %}
                <tr>
                    <td>{{ commentAggregate.author.nickname }}</td>
                    <td>{{ commentAggregate.comment.body }}</td>
                    <td>
                        <a href="/post?id={{ commentAggregate.postAggregate.postID.value }}">
                        {{ commentAggregate.postAggregate.header.title }}
                        </a>
                    </td>
                    <td>{{ commentAggregate.comment.createAt|date('d/m/Y H:i') }}</td>
                    <td>
                        <a href="#" class="btn btn-primary">Approuvé</a>
                        <a href="#" class="btn btn-danger">Modifier</a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{%  endblock content %}

{% block actionTitle %}Commentaires{% endblock actionTitle %}